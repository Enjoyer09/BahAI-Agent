# 2026-08-23

## Web slowness audit → code patches applied

User asked (AZ): "sen tapdigin butun bu sehfl ve buglari patch ede bilersen?" (can you patch all the bugs found?).
Interpreted "sehfl" = səhvlər (errors). Answer: yes for code-side, operator-only for deploy/env/token.

Patched in `backend/` (user is non-technical AZ speaker; explained plainly):
1. `routes/chat.js:694-699` — added UPPER cap on `LLM_FIRST_TOKEN_MS` / `VISION_LLM_FIRST_TOKEN_MS`
   (web 25s) so a stale high Railway value can't stall cold-provider failover.
2. `routes/chat.js` — new `getDirectWeatherReply()` fast-path: simple "Bakıda indi hava necədir?"
   queries skip the 3-step agent loop + web_search and answer directly from wttr.in (5s).
   Forecast/multi-day/unknown-city fall through to the agent. Called inside `getDirectWebChatReply`
   before its final `return ''`.
3. `toolRunner.js:324` — wttr.in timeout 10000 → 5000ms.

Tests: added `backend/tests/web-weather-fastpath.test.js` (4 tests, global.fetch mocked).
Verification: `node --check` both files OK; 27 unit tests pass (weather 4, stream 12 incl. (b) leak guard,
web-chat-guards 3, chat-queue). The `gui-fastpath.test.js` server-boot-timeout failure is PRE-EXISTING
(confirmed identical failure on a clean `git stash` tree) → sandbox resource/port flakiness, NOT my change.

NOT done (operator-only): commit + Railway redeploy (dist is stale), set Railway env
(`LLM_FIRST_TOKEN_MS=15000`, `LLM_TIMEOUT_CHAT=20000`), rotate leaked token `sk-153edf3948fcef97-...`.
Deferred (behavior-changing, needs user OK): soften "HƏMİŞƏ DƏRHAL web_search" mandate (chat.js:517);
parallel search backends (Promise.any).

Audit report updated: `artifacts/web-slowness-audit.md` (added "Patch vəziyyəti" section).
